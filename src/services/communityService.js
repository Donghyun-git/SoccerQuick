const { Post, Comment } = require('../model/models/index');
const { AppError } = require('../middlewares/errorHandler');

//[ 모든 게시글 조회 ]
/** (컨트롤러에서 필요한 데이터를 받아 파라미터 넘겨서 로직 수행하고 반환해야함) */
const getAllPosts = () => {};
module.exports = {
  getAllPosts,
};
